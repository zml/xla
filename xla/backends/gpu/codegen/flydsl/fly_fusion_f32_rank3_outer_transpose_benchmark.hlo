HloModule fly_f32_rank3_outer_transpose_benchmark

transpose {
  p0 = f32[256,1024,64]{2,1,0} parameter(0)
  ROOT result = f32[1024,256,64]{2,1,0} transpose(p0),
    dimensions={1,0,2}
}

ENTRY main {
  p0 = f32[256,1024,64]{2,1,0} parameter(0)
  ROOT fusion = f32[1024,256,64]{2,1,0} fusion(p0), kind=kLoop,
    calls=transpose
}
