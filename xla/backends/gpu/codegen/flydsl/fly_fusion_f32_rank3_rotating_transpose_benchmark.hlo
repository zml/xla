HloModule fly_f32_rank3_rotating_transpose_benchmark

transpose {
  p0 = f32[256,1024,64]{2,1,0} parameter(0)
  ROOT result = f32[64,256,1024]{2,1,0} transpose(p0),
    dimensions={2,0,1}
}

ENTRY main {
  p0 = f32[256,1024,64]{2,1,0} parameter(0)
  ROOT fusion = f32[64,256,1024]{2,1,0} fusion(p0), kind=kLoop,
    calls=transpose
}
